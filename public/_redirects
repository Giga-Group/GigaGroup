# Return 404 for old career page URLs (removed from site)
/careers  /404  404
/career   /404  404

# Redirect old project URLs to /project/:slug
/project-single/*  /project/:splat  301

# SPA fallback so /project/:slug and other client routes load the app
/*    /index.html   200
